// 函数入口: sub_140236280 - 检查是否需要对压缩动画数据进行解压
// 功能: 判断给定的动画轨道数据是否需要解压处理
// 参数:
//   a1: AnimationSourceDecompressor对象指针，用于存储解压参数
//   a2: 压缩元数据结构指针
//   a3: 解压参数数组指针
// 返回: char (0=不需要解压, 1=需要解压)
//
// 该函数是0x3409压缩算法的入口判断函数，主要逻辑：
// 1. 调用sub_140233810检查压缩类型
// 2. 如果返回值为1，则需要解压；否则直接返回0
// 3. 根据帧数决定使用简单插值还是完整解压
//
// 这是整个解压流程的关键入口点，决定了后续是否进行0x3409解压处理
char __fastcall sub_140236280(__int64 a1, __int64 a2, int *a3)
{
  __int64 v4; // rbp - 保存a2参数
  __int64 v6; // rbx - AnimationSourceDecompressor + 8的指针
  __int64 v7; // rcx - 当前对象的引用
  __int64 v9; // rdx - 压缩元数据指针
  int v10; // ecx - 帧数检查变量
  __int64 v11; // rax - 函数调用返回值
  _WORD *v12; // rcx - 字符串指针
  unsigned __int16 v13; // ax - 字符串偏移
  int v14; // ecx - 解压参数值
  __int64 v15; // rdx - 临时变量
  __int64 v16; // rdi - 解压配置对象
  __int64 v17; // rcx - 引用计数指针
  char *v18; // rdx - 临时变量
  __int64 v19[4]; // [rsp+20h] [rbp-88h] BYREF - 异常处理结构
  char v20[56]; // [rsp+40h] [rbp-68h] BYREF - 临时缓冲区
  char *v21; // [rsp+78h] [rbp-30h] - 临时对象指针

  v19[3] = -2i64;                           // 初始化异常处理结构

  v4 = a2;                                  // 保存参数a2

  // 初始化AnimationSourceDecompressor对象
  *(_DWORD *)a1 = 0;                        // 清空解压参数
  v6 = a1 + 8;                              // 计算对象内部指针

  // 清理现有的解压配置对象
  v7 = *(_QWORD *)(a1 + 64);                // 获取当前配置对象引用
  if ( v7 )                                 // 如果有引用
  {
    LOBYTE(a2) = v7 != v6;                  // 确定是否为最后一个引用
    (*(void (__fastcall **)(__int64, __int64))(*(_QWORD *)v7 + 32i64))(v7, a2); // 释放引用
    *(_QWORD *)(v6 + 56) = 0i64;            // 清空引用计数
  }

  // ===== 关键判断：检查是否需要解压 =====
  // 调用sub_140233810检查压缩类型
  // 如果返回值不等于1，表示数据不需要解压，直接返回0
  if ( (unsigned int)sub_140233810(*(_QWORD *)(v4 + 8), a3) != 1 )
    return 0;                               // 返回0：不需要解压

  // ===== 数据需要解压，继续处理 =====

  v9 = *(_QWORD *)(v4 + 8);                 // 获取压缩元数据指针

  // 检查帧数信息
  v10 = *(_DWORD *)(v9 + 24);               // 读取帧数
  if ( v10 < 0 )                            // 处理负数情况
    v10 = ~v10;                             // 取反
  if ( !v10 )                               // 如果帧数为0
    goto LABEL_12;                          // 跳转到默认处理

  // 获取帧数信息
  v11 = sub_14023C1E0(v9 + 24);             // 调用函数获取帧数
  if ( !v11 )                               // 如果调用失败
  {
    // 构造异常对象并抛出
    v19[1] = 0i64;
    v19[2] = 0i64;
    v19[0] = (__int64)&boost::bad_get::`vftable';
    sub_1400D7330(v19);                     // 抛出异常
  }

  // 根据帧数决定解压策略
  if ( *(_WORD *)(*(_QWORD *)v11 + 4i64) <= 2u ) // 如果帧数<=2
  {
    // 简单情况：直接使用参数数组的值
    *(_DWORD *)a1 = a3[2];                  // 设置解压参数为a3[2]
  }
  else                                      // 帧数>2，需要完整解压
  {
LABEL_12:
    // 从压缩数据结构中提取解压参数
    v12 = (_WORD *)((char *)a3 - *a3);      // 计算字符串基地址
    if ( *v12 > 0xCu && (v13 = v12[6]) != 0 ) // 检查字符串长度和偏移
      v14 = *(int *)((char *)a3 + v13);     // 从偏移位置读取参数
    else
      v14 = 0;                              // 默认值
    *(_DWORD *)a1 = v14;                    // 设置解压参数
  }

  // 设置解压配置对象
  v16 = sub_1402377A0(v20, *(_QWORD *)(v4 + 8), a3); // 创建解压配置

  // 绑定配置对象到AnimationSourceDecompressor
  if ( v6 != v16 )                          // 如果配置不同
  {
    v17 = *(_QWORD *)(v6 + 56);             // 获取当前引用
    if ( v17 )                              // 如果有引用
    {
      LOBYTE(v15) = v17 != v6;              // 确定释放模式
      (*(void (__fastcall **)(__int64, __int64))(*(_QWORD *)v17 + 32i64))(v17, v15); // 释放引用
      *(_QWORD *)(v6 + 56) = 0i64;          // 清空引用计数
    }
    sub_14003A0E0(v6, v16);                 // 绑定新配置
  }

  // 清理临时对象
  if ( v21 )                                // 如果临时对象存在
  {
    v18 = v20;                              // 设置参数
    LOBYTE(v18) = v21 != v20;               // 确定释放模式
    (*(void (__fastcall **)(char *, char *))(*(_QWORD *)v21 + 32i64))(v21, v18); // 释放临时对象
  }

  return 1;                                 // 返回1：需要解压
}

// ===== 解压判断逻辑详解 =====
//
// 核心判断条件：
// if ( (unsigned int)sub_140233810(*(_QWORD *)(v4 + 8), a3) != 1 )
//     return 0;
//
// 这个判断决定了整个解压流程：
// - sub_140233810函数检查数据格式和压缩类型
// - 如果返回1：数据是0x3409格式，需要解压
// - 如果返回其他值：数据不需要解压（可能是未压缩或不同格式）
//
// 帧数处理策略：
// - 帧数 <= 2: 使用简单插值，直接从参数读取值
// - 帧数 > 2: 使用完整的0x3409解压算法
//
// 这个函数是整个压缩动画处理流程的入口点。
