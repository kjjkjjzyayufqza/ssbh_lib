# nu_DecompressCurve_0x3409 准确复现难点分析

本文档旨在识别并解释在准确复现 `nu_DecompressCurve_0x3409` 函数（地址 `0x140412DF0`）的动画解压算法时遇到的核心技术瓶颈。

## 1. 核心瓶颈：SIMD 矩阵操作的黑箱 (已解决：矩阵常数来自 Nuanmb 文件)

尽管我们成功获取了 `sub_140412DF0` 的伪代码，并确认了其使用了 Hermite 样条或类似的三次曲线插值，但精确复现 Level 3 转换仍存在瓶颈。通过 IDA Pro MCP 分析，我们确定了 SIMD 辅助函数的功能和矩阵的真正来源。

### 1.1 依赖外部 SIMD 辅助函数

`nu_DecompressCurve_0x3409` 函数严重依赖两个外部 SIMD 辅助函数。通过反编译，我们确定了它们的功能：

| 函数名称 | 地址 | 实际功能 | 难度 |
| :--- | :--- | :--- | :--- |
| `sub_14006A5C0` | `0x14006A5C0` | SIMD 矩阵-向量乘法 (MADD) | 已解决 |
| `sub_14013DAD0` | `0x14013DAD0` | 齐次坐标规范化/投影除法 | 已解决 |
| `sub_1400C7EA0` | `0x1400C7EA0` | 核心 $V_{\text{input}} \times M$ 乘法 | 已解决 |

### 1.2 关键插值公式的 SIMD 混淆

样条插值（如 Hermite）通常涉及时间 $t$ 的幂 $t, t^2, t^3$ 组成的向量与一个 $4 \times 4$ 基矩阵的乘法。在 IDA 伪代码中，这些操作被高度优化的 SIMD 指令（如 Line 280 的 `_mm_add_ps`, `_mm_mul_ps`, `_mm_sub_ps` 等）所取代，掩盖了底层的数学公式。

复现的关键在于确定以下几点：
1. **基矩阵 (Base Matrix):** **已发现。** Level 3 转换所需的 $4 \times 4$ 矩阵 **不是硬编码常数**，而是动态从 **Nuanmb 文件的数据区域**加载的。
2. **输入向量 (Time Vector):** 确定时间参数 $t$ 如何被转换为 SIMD 寄存器中的四维向量。
3. **非线性系数:** 伪代码中存在的浮点数常量（例如 Lines 216-218 的 `-25.0`, `-20.0` 和 Lines 247-249 的 `1.2`, `1.3`）是用于计算 Level 3 混合所需的 **输入向量 $V_{\text{input}}$** 的配置参数。 Level 3 的最终混合公式 `v74` (Line 280) 中还使用了硬编码的 $0.3$ 混合系数。

## 2. 数据格式的歧义：38个浮点数的含义

`3409.bin` 文件包含 38 个浮点数。我们知道第 1 个是 $P_{start}$，第 7 个是 $P_{end}$，第 4 个 ($71.693703$) 是某个控制点或因子。

**难点：**
对于跨越 40 帧的单段曲线而言：
*   **标准 Hermite** 只需要 $P_0, T_0, P_1, T_1$ (4个值)。
*   **标准 Bezier** 只需要 $P_0, C_1, C_2, P_1$ (4个值)。

38 个浮点数远超单段曲线所需。这意味着：
a) **这些浮点数是多段分段曲线的控制点**：例如，每 4 个浮点数定义一个 Hermite 段 (19个段? 不可能，因为只有 40 帧)。
b) **只有其中极少数用于曲线定义**：大多数浮点数是未使用的数据或用于定义其他轴。
c) **它们是用于 SIMD 预加载的复杂数据结构**：38个浮点数可能代表了多个 $4 \times 4$ 矩阵，它们被预加载进 SIMD 寄存器链作为插值计算的上下文。这与伪代码中加载 `v69`, `v70`, `v71`, `v72` (以及 `v75`, `v76`, `v77`, `v78`) 的过程吻合，但我们不知道哪个浮点数对应哪个矩阵分量。

我们当前的 Python 脚本通过“启发式”方式提取 $P_0, P_1$ 并猜测切线 $T$ 因子，这证明了**精确的数学模型尚未被识别**。

## 3. 下一步行动建议

要获得准确的转换算法，我们必须克服上述瓶颈，这通常要求在 IDA 环境中进行更深入的分析：

1. **识别矩阵结构:** 尝试通过 IDA 的内存查看功能，识别在运行时加载到 `v69`-`v72` 和 `v75`-`v78` 的 **精确浮点数值**。
2. **分析辅助函数:** 尽可能完整地反编译和理解 `sub_14006A5C0` 和 `sub_14013DAD0`，甚至在其中查找硬编码的 $4 \times 4$ 矩阵常量。
3. **精确映射数据:** 根据确定的矩阵结构，将 `3409.bin` 中的 38 个浮点数精确地映射到其在 SIMD 寄存器中的作用。

**结论:** 难点在于 Level 3 转换矩阵 $M_1/M_2$ 的数值来源于 **Nuanmb 文件的动态数据块**，而非程序硬编码的常量。原始的 SIMD 矩阵操作和辅助函数已不再是黑箱。
