// 函数入口: sub_1402364E0 - 计算0x3409压缩数据的解压参数
// 功能: 计算解压因子和其他压缩参数，用于后续的动画数据解压
// 参数:
//   a1: AnimationSourceDecompressor对象指针
//   a2: 压缩元数据结构指针
//   a3: 解压参数指针
// 返回: 布尔值，表示是否需要进行解压
//
// 该函数在sub_14023F830中被调用，用于设置关键的解压参数：
// *(float *)(a1 + 72) = (sub_1402338B0(v7, a3) - 1) / sub_140239FE0(v7)
//
// 这个计算结果影响后续的位流解码和插值计算
__int64 __fastcall sub_1402364E0(__int64 a1, __int64 a2, __int64 a3)
{
  unsigned __int8 v6; // si - 返回值，指示是否需要解压
  __int64 v7; // rdi - 压缩元数据指针
  int v8; // edx - 临时变量，用于帧数检查
  __int64 v9; // rax - 函数调用返回值
  float v10; // xmm6_4 - 计算出的解压参数
  __int64 v11; // rdx - 临时变量
  __int64 v12; // rdi - 函数调用返回值
  __int64 v13; // rbx - 目标对象指针
  __int64 v14; // rcx - 引用计数指针
  char *v15; // rdx - 临时变量
  __int64 v17[4]; // [rsp+20h] [rbp-98h] BYREF - 异常处理结构
  char v18[56]; // [rsp+40h] [rbp-78h] BYREF - 临时缓冲区
  char *v19; // [rsp+78h] [rbp-40h] - 临时对象指针

  v17[3] = -2i64;                           // 初始化异常处理结构

  // 检查是否需要进行解压处理
  v6 = sub_140236280();                     // 调用检查函数，确定是否需要解压
  if ( v6 )                                 // 如果需要解压
  {
    v7 = *(_QWORD *)(a2 + 8);              // 获取压缩元数据结构指针

    // 检查帧数信息
    v8 = *(_DWORD *)(v7 + 24);             // 读取帧数或相关计数
    if ( v8 < 0 )                          // 处理负数情况（可能是标志位）
      v8 = ~v8;                            // 取反得到正数
    if ( !v8 )                             // 如果计数为0
      goto LABEL_9;                        // 跳转到默认计算分支

    // 获取帧数信息
    v9 = sub_14023C1E0(v7 + 24);           // 调用函数获取帧数或时间信息
    if ( !v9 )                             // 如果调用失败
    {
      // 构造异常对象
      v17[1] = 0i64;
      v17[2] = 0i64;
      v17[0] = (__int64)&boost::bad_get::`vftable'; // 设置异常虚表
      sub_1400D7330(v17);                  // 抛出异常
    }

    // 检查帧数是否足够
    if ( *(_WORD *)(*(_QWORD *)v9 + 4i64) < 2u ) // 如果帧数小于2
    {
      *(_DWORD *)(a1 + 72) = 1114636288;   // 设置默认值 (60.0f)
    }
    else                                   // 帧数足够，进行正常计算
    {
LABEL_9:
      // ===== 关键计算：解压参数计算 =====
      // 公式: (sub_1402338B0(v7, a3) - 1) / sub_140239FE0(v7)
      // 这个值存储在AnimationSourceDecompressor的偏移72处
      // 影响后续的位流解码精度和插值计算
      v10 = (float)(int)(sub_1402338B0(v7, a3) - 1); // 获取索引范围，减1后转换为float
      *(float *)(a1 + 72) = v10 / sub_140239FE0(v7);  // 除以归一化因子
    }

    // 设置其他解压参数
    v12 = sub_140237960(v18);              // 获取解压配置对象
    v13 = a1 + 80;                         // 目标位置：AnimationSourceDecompressor + 80

    // 替换现有的解压配置
    if ( v13 != v12 )                      // 如果配置不同
    {
      v14 = *(_QWORD *)(v13 + 56);         // 获取当前对象的引用计数
      if ( v14 )                           // 如果有引用
      {
        LOBYTE(v11) = v14 != v13;          // 确定是否为最后一个引用
        (*(void (__fastcall **)(__int64, __int64))(*(_QWORD *)v14 + 32i64))(v14, v11); // 释放引用
        *(_QWORD *)(v13 + 56) = 0i64;      // 清空引用计数
      }
      sub_14003A0E0(v13, v12);             // 绑定新的解压配置对象
    }

    // 清理临时对象
    if ( v19 )                             // 如果临时对象存在
    {
      v15 = v18;                           // 设置参数
      LOBYTE(v15) = v19 != v18;            // 确定释放模式
      (*(void (__fastcall **)(char *, char *))(*(_QWORD *)v19 + 32i64))(v19, v15); // 释放临时对象
    }
  }

  return v6;                               // 返回解压标志
}

// ===== 关键计算详解 =====
// *(float *)(a1 + 72) = (sub_1402338B0(v7, a3) - 1) / sub_140239FE0(v7)
//
// 其中:
// - sub_1402338B0(v7, a3): 获取索引值的最大范围 (通常是2^bits_per_entry - 1)
// - sub_140239FE0(v7): 获取归一化因子 (通常是2^bits_per_entry - 1)
//
// 这个计算将索引范围转换为[0,1]区间的浮点值，用于后续的插值计算