# v110数组内容分析 - 基于给定的数据样本

## 样本数据分析

给定的数据：
```
09 34 00 00 28 00 00 00 00 00 80 3F 5B FE E6 41 02 00 15 00 00 00 00 00 00 00 00 00 B9 C2 4B 3F 00 00 00 00 00 00 00 00 2D 63 8F 42 00 00 00 00 00 00 00 00 7F 59 B5 42 01 00 01 00 01 00 08 12 01 00 01 00 01 00 08 12 FF FF E3 0B 58 71 00 12 01 80 29 35 A7 0F 9A 0E 04 60 FD 7F 7A 31 03 43 A6 1B E4 2B B6 0E 01 20 36 18 8B 44 6A 0F 49 34 8B 08 CB 2A 5E 02 43 24 72 FE 36 1E 50 FC 2D 19 0F FA 01 16 4B F7 A6 13 F5 11 F4 0E
```

解析后的header：
- Magic: 0x00003409
- Frame count: 40 (0x28)
- Unknown value 1: 1.0
- Unknown value 2: 28.874197
- Flags: 2
- Bits per entry: 21 (0x15)

## v110数组计算过程

### 1. 循环结构
```c
for (v59 = 0; v59 < track_component_count; v59++) {
    if (v63 = v132[v59]) {  // 如果当前分量有数据
        // 计算帧偏移: (v63 - base_offset) / 312
        frame_offset = (v63 - *(_QWORD *)(v62 + 24)) / 312;

        // 调用索引计算函数
        v110[v59] = sub_14023F760(v109, v88, frame_offset);
    } else {
        v110[v59] = 0;  // 无数据时设为0
    }
}
```

### 2. sub_14023F760计算逻辑

```c
unsigned __int64 sub_14023F760(_QWORD *a1, unsigned int a2, unsigned int a3) {
    _QWORD *v5 = (_QWORD *)a1[3];  // Unknown Value 1
    if (!v5 || *v5 != a1[4])       // 验证Unknown Value 2
        return 0i64;

    __int64 v7 = a1[2];            // AnimationNodePlugLayout对象
    if ((unsigned __int64)a2 >= *(_QWORD *)(v7 + 72))  // 检查帧索引范围
        return 0i64;

    // 计算索引值
    if (*(_BYTE *)(v9 + 2993)) {
        v11 = a2 * v10[15] + *(_DWORD *)(*(_QWORD *)(v9 + 2648) + 4 * a3);
    } else {
        v11 = *v10 + a2 * (unsigned __int64)*(unsigned int *)(v13 + 16);
    }

    return *a1 + v11;  // 返回基地址 + 计算偏移
}
```

### 3. 对于40帧数据的v110内容

**假设场景**：
- Transform轨道（位移+旋转+缩放 = 9个分量）
- v88 = 当前帧索引 (0-39)
- 312 = 每个动画帧数据结构的大小
- v62+24 = 轨道数据基地址

**v110数组可能的结构**：
```c
v110[0] = 轨道基地址 + (帧索引 * 缩放因子1) + 偏移1  // X位移
v110[1] = 轨道基地址 + (帧索引 * 缩放因子1) + 偏移2  // Y位移
v110[2] = 轨道基地址 + (帧索引 * 缩放因子1) + 偏移3  // Z位移
v110[3] = 轨道基地址 + (帧索引 * 缩放因子2) + 偏移4  // X旋转
v110[4] = 轨道基地址 + (帧索引 * 缩放因子2) + 偏移5  // Y旋转
v110[5] = 轨道基地址 + (帧索引 * 缩放因子2) + 偏移6  // Z旋转
v110[6] = 轨道基地址 + (帧索引 * 缩放因子2) + 偏移7  // W旋转
v110[7] = 轨道基地址 + (帧索引 * 缩放因子3) + 偏移8  // X缩放
v110[8] = 轨道基地址 + (帧索引 * 缩放因子3) + 偏移9  // Y缩放
v110[9] = 轨道基地址 + (帧索引 * 缩放因子3) + 偏移10 // Z缩放
```

### 4. 实际计算示例

对于第0帧 (v88 = 0)：

```
frame_offset = (current_frame_data_ptr - base_offset) / 312

// 如果使用第一种计算方式：
v11 = 0 * v10[15] + *(_DWORD *)(track_base + 4 * frame_offset)

// 如果使用第二种计算方式：
v11 = *v10 + 0 * track_stride

v110[component_index] = track_base_address + v11
```

### 5. 关键参数影响

- **Bits per entry (21)**: 影响v10[15]或track_stride的计算
- **Unknown value 2 (28.874197)**: 用于数据验证，确保轨道数据完整性
- **Flags (2)**: 影响计算方式的选择（第一种或第二种公式）
- **Frame count (40)**: 限制v88的最大值

### 6. v110的最终用途

v110数组被传递给插值函数，用于：
- 从压缩数据流中定位具体的动画值
- 确定插值算法的参数
- 计算最终的动画变换矩阵

每个v110[i]值都是一个内存地址偏移量，指向压缩数据中对应的分量值，用于后续的解压和插值计算。
