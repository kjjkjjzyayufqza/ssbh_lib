# 0x3409 Header字段详细使用分析

基于对`sub_1402407B0`和相关函数的反编译分析，以下是0x3409压缩格式header中四个字段的具体使用方式：

## Header字段对应关系

### 1. Frame Count (帧数量) - 4字节
**Header偏移**: 4-7字节
**存储位置**: `v15 + 72` (从`sub_14006D1E0`返回的AnimationNodePlugLayout对象)
**使用方式**:
```c
v16 = *(_QWORD *)(v15 + 72);  // 读取帧数量
v99 = v16;                    // 存储到v99用于后续循环
```

### 2. Unknown Value 1 (1.0) - 4字节
**Header偏移**: 8-11字节
**存储位置**: `v107` (从`sub_140071D50`返回)
**使用方式**:
- 作为压缩元数据存储在动画轨道结构中
- 在`v100[1] = v102`中设置，用于动画处理参数

### 3. Unknown Value 2 (28.874197) - 4字节
**Header偏移**: 12-15字节
**存储位置**: `v108` (从`sub_140071D50`返回)
**使用方式**:
- 作为压缩元数据存储在动画轨道结构中
- 在`v100[2] = v103`中设置，用于动画处理参数

### 4. Flags (2) - 2字节
**Header偏移**: 16-17字节
**使用方式**:
- 控制压缩算法选择和处理流程
- 通过条件检查影响轨道处理函数的选择：
```c
if (*(_DWORD *)(a2 + 80) == 823 && *_DWORD *(a2 + 168) == 268436287 && ...)
    // 使用特殊lambda函数处理
else
    // 使用标准插值算法
```

### 5. Bits Per Entry (21) - 2字节
**Header偏移**: 18-19字节
**存储位置**: 轨道结构中的特定偏移 (a2 + 384, a2 + 392)
**使用方式**:
```c
sub_1402364E0(a1, *(_QWORD *)(a2 + 384), *(_QWORD *)(a2 + 392));
// a2+384包含bits per entry值，a2+392包含其他压缩参数

// 在sub_1402364E0中：
v10 = (float)(int)(sub_1402338B0(v7, a3) - 1);
*(float *)(a1 + 72) = v10 / sub_140239FE0(v7);
// bits per entry直接影响解压参数计算，确定索引范围
```

## 数据流分析

```
原始0x3409数据 → sub_14006D1E0解析 → AnimationNodePlugLayout对象
                                      ↓
                            field_72: Frame Count
                            其他字段: 传递给轨道处理

轨道初始化 → sub_140071D50 → v107/v108变量
                                      ↓
                            Unknown Value 1/2存储在v100数组

轨道数据 → 包含压缩参数 → sub_14023F760(v109, v88, frame_offset/312)
                            ↓
                   v110[v59]: 计算出的帧索引值数组
                   每个元素 = *a1 + v11 (a1为轨道基地址 + 计算偏移)
                            ↓
                   传递给插值函数 (sub_1402423F0等)
                            ↓
                   a2+384: Bits Per Entry (21)
                   a2+392: 其他压缩参数
                   a2+40/a2+80: v110[]中的索引值
                   条件检查: Flags(2)控制算法选择
```

## 关键函数调用链

1. **Header解析**: `sub_14006D1E0(a1指向0x3409数据, v105)` - 创建AnimationNodePlugLayout对象
2. **组件处理**: `sub_140071D50(v106, v101)` - 解析更多元数据，设置v107/v108
3. **轨道初始化**: `v109[4] = v108` - Unknown Value 2存储在轨道结构中
4. **帧索引计算**: `sub_14023F760(v109, v88, frame_index)` - 使用v109[4]进行验证，返回索引到v110[]
5. **插值处理**: `sub_1402423F0(v128, v110, 0)` - v110[]传递给插值函数
6. **参数计算**: `sub_1402364E0(a1, bits_per_entry, 其他参数)` - 最终解压参数计算

## 使用影响

- **Frame Count**: 确定动画循环次数和数据边界，存储在AnimationNodePlugLayout.field_72
- **Bits Per Entry**: 控制解压精度，影响位读取宽度，直接影响索引值范围计算
- **Flags**: 决定使用哪种插值算法（特殊lambda vs 标准算法），通过条件检查控制处理流程
- **Unknown Value 1**: 作为压缩元数据存储，用于轨道结构验证 (v109[3])
- **Unknown Value 2**: 作为压缩元数据存储，用于轨道结构验证 (v109[4])，影响帧索引计算和插值处理

这种设计允许每个轨道独立配置压缩参数，实现灵活的动画压缩方案。
