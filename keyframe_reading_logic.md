# 0x3409关键帧数据读取逻辑分析

## 关键帧数据位置

根据0x3409格式规范，三组关键帧数据位于文件的偏移20-56处：

```
偏移20-31: First keyframe  - 00 00 00 00 00 00 00 00 B9 C2 4B 3F
偏移32-43: Middle keyframe - 00 00 00 00 00 00 00 00 2D 63 8F 42
偏移44-55: Last keyframe   - 00 00 00 00 00 00 00 00 7F 59 B5 42
```

每个关键帧12字节（X、Y、Z三个float），总共36字节。

## 读取逻辑分析

### 1. 数据起始位置定位

```c
// sub_1402407B0中调用
sub_14006D1E0(*(_QWORD *)(*(_QWORD *)(a2 + 8) + 56i64), v105);

// 这意味着：
// a1 = 某个动画数据结构的偏移56处
// a1指向0x3409压缩数据的起始位置
```

### 2. 关键帧数据读取入口

```c
// sub_14006D1E0中
v3 = (_QWORD *)sub_14006D490(a1 + 144, v8);

// 关键发现：
// a1 + 144 = 0x3409数据起始位置
// 这意味着0x3409数据在某个更大结构中的偏移144处
```

### 3. 数据处理管道

```
sub_14006D490(a1 + 144, v8)
    ↓
sub_14006E6C0(a2, &v4, a1)
    ↓
sub_14006EE80(a3, a1, a2)
    ↓
sub_14006F090(a1, a2, a3, a4)  // 处理数据类型
    ↓
sub_14006F960(a1, a2, a3, a5)  // switch(a3)选择处理器
    ↓ case 0:
sub_14006FCC0(a1, a2, a3, a5)
    ↓
sub_14006FFE0(a1, a2, a3, a4)
    ↓
sub_1400704B0(a1, a1, a4)
    ↓
sub_140070710(a1, a2, a3)  // 创建AnimationNodePlugLayout对象
```

### 4. 关键帧数据读取点

关键帧数据的实际读取发生在**sub_14006F090**函数中：

```c
int v4 = *a1;  // 读取数据类型标识符
if (*a1 < 0)
    v4 = ~v4;
sub_14006F510(a2, a2, v4, a4, (__int64)(a1 + 2));
```

这里的`a1`指向0x3409数据起始位置，`a1 + 2`可能指向关键帧数据的开始。

### 5. 关键帧数据存储

从sub_14023F830函数的分析，关键帧数据被读取并存储在局部变量中：

```c
// sub_14023F830中的关键帧读取
v12 = *(_OWORD *)a2;      // 读取16字节 (可能包含first keyframe)
v13 = *(_OWORD *)(a2 + 16); // 读取16字节 (first + middle keyframe)
v14 = *(_QWORD *)(a2 + 32);  // 读取8字节
v15 = *(_OWORD *)(a2 + 40); // 读取16字节 (middle + last keyframe)
v16 = *(_OWORD *)(a2 + 56); // 读取16字节 (last keyframe剩余)

// 然后传递给插值函数
sub_14023FE60(&v12, v45);  // v45 = [v12, v13]
sub_140243B60(&v41, &v12); // 创建插值lambda函数
```

### 6. 读取逻辑总结

**读取位置**: `0x3409数据起始 + 20字节` (header后的36字节关键帧)

**读取方式**: 通过sub_14006D1E0的调用链，最终在sub_14023F830中作为a2参数传递

**存储方式**: 复制到局部变量v12-v16，然后传递给插值计算函数

**使用方式**: 用于segmented linear interpolation计算每个帧的动画值

### 7. 为什么是三组数据

这三组数据对应0x3409压缩算法的三个关键帧：
- **First**: 动画序列起点 (t=0.0)
- **Middle**: 动画序列中点 (t=0.5)
- **Last**: 动画序列终点 (t=1.0)

插值算法使用分段线性插值：
- t ∈ [0.0, 0.5]: 在First和Middle之间插值
- t ∈ [0.5, 1.0]: 在Middle和Last之间插值

这样可以提供更好的压缩质量，同时保持平滑的动画曲线。
